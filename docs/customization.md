# Customization

Edit CSS variables in `assets/css/style.css`, replace the inline logo or `logo.svg`, update repeated contact details, and modify each standalone HTML page directly. The new culinary journey, experience cards, values, timeline, private rooms, packages, location cards and journal filters are documented in `documentation.md`.
