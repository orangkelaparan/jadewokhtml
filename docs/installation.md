# Installation

Open `index.html` directly or serve the folder with `python3 -m http.server 8080`. Upload the folder contents to cPanel `public_html` or an FTP root. No npm, build step, database or server-side runtime is required.
