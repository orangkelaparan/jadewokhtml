# JavaScript

`assets/js/main.js` provides mobile navigation, sticky header, back-to-top, cookie notice, native form validation, filters, lightbox and testimonials. It is dependency-free and intentionally readable.
